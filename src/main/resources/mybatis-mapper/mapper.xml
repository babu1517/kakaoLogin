<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "https://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.block.kakaoLogin.Login.LoginService">
    <select id="findById" resultType="com.block.kakaoLogin.Login.UserDTO">
        select * from kakaologin where kakaoid = #{param1}
    </select>

</mapper>